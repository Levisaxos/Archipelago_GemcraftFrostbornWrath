package deathlink {
    // Placeholder — the EnragerOverride subclass approach requires the same
    // application domain as the game SWF, which Bezel does not provide.
    // Wave-surge virtual mode is handled via a hidden ghost gem in DeathLinkHandler.
}
